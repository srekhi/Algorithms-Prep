def longest_consecutive(root)
=begin
    Break this down into subproblems.
    Longest consecutive from root.right vs. longest consecutive root.left. If the root can be prepended to either, then
    add it and return
=end
    generate_longest_path(root, path = [])
    # 4 functions to consider
#    [generate_longest_consec(root.left, [])
 #   generate_longest_consec(root.right, [])
end

def generate_longest_consec(node, path)
    longest_path = []
    if root.right.val < root.val
      longest_path = [longest_path.length, lo]
    end 

end
